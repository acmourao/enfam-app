version: "3.9"
services:
  mysql:
    build: ./mysql/
    container_name: mysql
    ports:
      - 3306:3306
    volumes:
      - ./mysql-data/:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: educaenfam

volumes:
  mysql-data:
